# Specification

## Summary
**Goal:** Trigger a fresh build + deployment of the existing UniPlace Manager application to resolve the HTTP 400 error on the preview URL and provide a new working public preview link.

**Planned changes:**
- Trigger a fresh deployment (build + deploy) of the current UniPlace Manager project.
- Provide the newly generated public preview URL after deployment completes.

**User-visible outcome:** A new public preview URL that loads the UniPlace Manager application successfully in the browser (no HTTP 400).
